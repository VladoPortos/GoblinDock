# Direct dependencies (intent). The pinned lock is requirements.txt.
# Regenerate the lock with hashes:  uv pip compile requirements.in -o requirements.txt --generate-hashes
fastapi
starlette>=1.3.1  # security floor: GHSA-82w8-qh3p-5jfq (request.form() DoS), patched in 1.3.1
uvicorn[standard]
sqlmodel
proxmoxer
requests
argon2-cffi
passlib  # $6$ SHA-512-crypt for cloud-init chpasswd (stdlib `crypt` removed in Python 3.13)
cryptography
python-multipart
itsdangerous
PyYAML
paramiko
ansible-core
ansible-runner
APScheduler
